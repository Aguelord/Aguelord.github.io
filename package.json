{
  "name": "atelier-aguelord",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "sync": "node scripts/sync-github.mjs",
    "build": "astro build",
    "test": "node --test",
    "preview": "astro preview --host 127.0.0.1"
  },
  "dependencies": { "astro": "^6.0.0" },
  "engines": { "node": ">=22.12.0" }
}
